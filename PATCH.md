# Source zip

`loreforge-source.zip` is the current Loreforge snapshot (nested card bodies in brainstorm context, 9:16 portraits, library grouping).

Unzip and run:

```bash
unzip loreforge-source.zip
npm install
npm run dev
```

Or overlay just the context fix on an older unzip:

https://raw.githubusercontent.com/DJC43/loreforge/main/src/lib/context.ts
